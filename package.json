{
  "name": "pb-media-fetch-youtube-downloader",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "test": "npm --prefix frontend test && npm --prefix cloudflare-worker test"
  },
  "workspaces": [
    "frontend"
  ]
}
