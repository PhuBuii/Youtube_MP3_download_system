{
  "name": "sonicfetch-cors-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev worker.js --local --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
